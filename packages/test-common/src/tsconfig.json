{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "declarationDir": "../dist",
    // Required by Rollup (consumed by Rollup in the *-plugin packages).
    "module": "esnext",
    "types": ["jest", "node"]
  },
  "include": ["."]
}
