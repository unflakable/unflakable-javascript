{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Removes DOM types.
    "lib": ["ES2019"],
    // Avoids conflicting global definitions from, e.g., jest.
    "types": ["node"]
  },
  "include": [".eslintrc.js"]
}
